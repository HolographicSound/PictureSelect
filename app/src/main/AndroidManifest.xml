<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zero.pictureselect">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".app.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PictureSelectActivity"
            android:label="图片选择界面"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".PicturePreviewActivity"
            android:label="大图预览界面"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".ImageCropActivity"
            android:label="图片裁剪"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".LocalClipImageView.ImageCropActivity2"
            android:label="图片裁剪"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
    </application>
</manifest>